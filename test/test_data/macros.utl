[% macro fred;
  a = b + 3;
  echo;
  echo a;
  end;
  really.really.quite.useless('php');
  wilma(1, 2, 3);

macro fairly.useless();
  echo "useless";
  end;

macro really.really.quite.useless(arg1);
  echo arg1;
  end;
  
  %]
