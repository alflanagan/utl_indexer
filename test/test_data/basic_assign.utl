[%
  echo a + b;
  echo 5 - 7;
  echo "barney rubble" | html;
  /* identifier that begins with operator */
  sally.isfine = 12 / 6;
  sally.fineis = 'bat';
  fred = sally is fine;
  /* keyword as part of identifier */
/*  fred.breakthis = 0;
  fred.thisbreak = 2;
  empty = "";
  barney = fred * 3; %]
 here's some text
 [%
  abool = 3 == 4;
  a = 3;
  b = fred;
  wilma = e[12];
  c = a * (a + b) * b;
  d = a * a + b * b;
  e = a % b - b % c;
  default a = 3;
  default b = fred;
  c -= sally;
  d *= 7;
  d /= 3;
  default e = a % b - b % c;
  array = [1, 2, 'fred', 3.0, wilma(7)|fred];
  obj = ['one':2, 'two':3];
  obj.one = 3;
  obj['two'] = 7;
  n = [];
  child_image = asset.items('type':'image')[0]
  */
%]