[% if fred != wilma;
   echo "oops";
   elseif fred == betty;
   end; %]
[% if 2 == 3 then "hello" %]
[%
  if navigation == 'mega' && nav_item.children.length > 0 && item_mega_excluded != "true";  else; parent_none_check="display:none;"; end;
  if silly == test;
  else;
    empty = statement_list;
  end;
%]
